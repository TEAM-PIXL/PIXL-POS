package teampixl.com.pixlpos.database.api;

public enum StatusCode {
    SUCCESS,
    USER_ALREADY_EXISTS,
    USER_NOT_FOUND,
    USER_UPDATE_FAILED,
    INVALID_USERNAME,
    INVALID_EMAIL,
    INVALID_FIRST_NAME,
    INVALID_LAST_NAME
}